const toggle=document.querySelector('.menu-toggle'),menu=document.querySelector('.mobile-menu');function closeMenu(){toggle.classList.remove('open');menu.classList.remove('open')}toggle.addEventListener('click',()=>{toggle.classList.toggle('open');menu.classList.toggle('open')});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));addEventListener('scroll',()=>document.querySelector('.header').classList.toggle('scrolled',scrollY>35));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.isIntersecting&&entry.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const wave='<svg viewBox="0 0 1440 100" preserveAspectRatio="none" aria-hidden="true"><path d="M0 28c175 61 330 61 520 14 221-55 370-45 565 6 140 37 246 34 355 5v47H0Z"/></svg>';
[['.hero','from-hero to-cream'],['.promise','from-promise to-sea'],['.cocktails','from-menu to-night'],['.nights','from-nights to-cream'],['.voices','from-voices to-contact']].forEach(([selector,classes])=>document.querySelector(selector).insertAdjacentHTML('afterend',`<div class="sea-divider ${classes}">${wave}</div>`));
const booking=document.querySelector('#booking');document.querySelectorAll('[data-book]').forEach(button=>button.addEventListener('click',()=>{closeMenu();booking.showModal()}));booking.querySelector('.close').addEventListener('click',()=>booking.close());booking.addEventListener('click',event=>event.target===booking&&booking.close());booking.querySelector('form').addEventListener('submit',event=>{event.preventDefault();const data=new FormData(event.target),text=`Ciao Vida Loca! Vorrei prenotare un tavolo.%0A%0ANome: ${data.get('name')}%0AData: ${data.get('date')}%0APersone: ${data.get('people')}%0AMomento: ${data.get('occasion')}`;window.open(`https://wa.me/393286217581?text=${text}`,'_blank')});
if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const hero=document.querySelector('.hero-image');let queued=false;addEventListener('scroll',()=>{if(!queued){requestAnimationFrame(()=>{hero.style.transform=`translateY(${Math.min(scrollY,innerHeight)*.08}px) scale(1.02)`;queued=false});queued=true}},{passive:true})}
